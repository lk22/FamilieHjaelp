[build]
[build.variables]
RAILPACK_INSTALL_CMD = "npm install && npm run build"
RAILPACK_INSTALL_CMD = "composer install --optimize-autoloader --no-scripts --no-interaction --ignore-platform-req=ext-pcntl"
